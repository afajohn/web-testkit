const fs = require('fs');
const path = require('path');
const file = path.join(process.cwd(),'tests','url-audit.spec.ts');
let txt = fs.readFileSync(file,'utf8');
const regex = /const seoCheckPromise[\s\S]*?const functionalResults = await \(async \(\) => {[\s\S]*?}\)\(\);/m;
if(!regex.test(txt)) { console.error('pattern not found'); process.exit(1); }
const replacement = "console.log('🏁 STARTING SEQUENTIAL AUDIT (Stability Mode)...');\n\n      const seoStart = Date.now();\n      console.log('  [1/10] Running SEO checks...');\n      const seoResults = await runSEOChecks(page).catch((error) => {\n        const elapsed = ((Date.now() - seoStart) / 1000).toFixed(1);\n        console.error(  [1/10] ✗ SEO checks failed (s): );\n        throw error;\n      });\n      console.log(  [1/10] ✓ SEO checks completed (s));\n\n      const securityStart = Date.now();\n      console.log('  [2/10] Running security checks...');\n      const securityResults = await runSecurityChecks(page, currentUrl).catch((error) => {\n        const elapsed = ((Date.now() - securityStart) / 1000).toFixed(1);\n        console.error(  [2/10] ✗ Security checks failed (s): );\n        throw error;\n      });\n      console.log(  [2/10] ✓ Security checks completed (s));\n\n      const accessibilityStart = Date.now();\n      console.log('  [3/10] Running accessibility scan...');\n      const accessibilityResults = await runAccessibilityCheckOnVisibleContent(page, { skipPageLoad: true }).catch((error) => {\n        const elapsed = ((Date.now() - accessibilityStart) / 1000).toFixed(1);\n        console.error(  [3/10] ✗ Accessibility check failed (s): );\n        throw error;\n      });\n      console.log(  [3/10] ✓ Accessibility check completed (s));\n\n      const brokenStart = Date.now();\n      console.log('  [4/10] Checking broken links...');\n      const brokenLinksResult = await checkBrokenLinks(page, apiRequest, undefined, 10, true).catch((error) => {\n        const elapsed = ((Date.now() - brokenStart) / 1000).toFixed(1);\n        console.error(  [4/10] ✗ Broken links check failed (s): );\n        throw error;\n      });\n      console.log(  [4/10] ✓ Broken links check completed (s));\n\n      const structuredStart = Date.now();\n      console.log('  [5/10] Starting structured data checks...');\n      const structuredDataResults = await checkStructuredData(page).catch((error) => {\n        const elapsed = ((Date.now() - structuredStart) / 1000).toFixed(1);\n        console.error(  [5/10] ✗ Structured data checks failed (s): );\n        throw error;\n      });\n      console.log(  [5/10] ✓ Structured data checks completed (s));\n\n      const socialStart = Date.now();\n      console.log('  [6/10] Starting social media checks...');\n      const socialMediaResults = await checkSocialMediaTags(page).catch((error) => {\n        const elapsed = ((Date.now() - socialStart) / 1000).toFixed(1);\n        console.error(  [6/10] ✗ Social media checks failed (s): );\n        throw error;\n      });\n      console.log(  [6/10] ✓ Social media checks completed (s));\n\n      const extendedStart = Date.now();\n      console.log('  [7/10] Starting extended SEO checks...');\n      const extendedSEOResults = await runExtendedSEOChecks(page, currentUrl, apiRequest).catch((error) => {\n        const elapsed = ((Date.now() - extendedStart) / 1000).toFixed(1);\n        console.error(  [7/10] ✗ Extended SEO checks failed (s): );\n        throw error;\n      });\n      console.log(  [7/10] ✓ Extended SEO checks completed (s));\n\n      const formStart = Date.now();\n      console.log('  [8/10] Starting form validation checks...');\n      const formValidationResults = await validateForms(page).catch((error) => {\n        const elapsed = ((Date.now() - formStart) / 1000).toFixed(1);\n        console.error(  [8/10] ✗ Form validation checks failed (s): );\n        throw error;\n      });\n      console.log(  [8/10] ✓ Form validation checks completed (s));\n\n      const visualStart = Date.now();\n      console.log('  [9/10] Starting visual tests...');\n      const visualResults = await runVisualTests(page).catch((error) => {\n        const elapsed = ((Date.now() - visualStart) / 1000).toFixed(1);\n        console.error(  [9/10] ✗ Visual tests failed (s): );\n        throw error;\n      });\n      console.log(  [9/10] ✓ Visual tests completed (s));\n\n      const functionalStart = Date.now();\n      console.log('  [10/10] Starting functional tests...');\n      const functionalResults = await runFunctionalTests(page).catch((error) => {\n        const elapsed = ((Date.now() - functionalStart) / 1000).toFixed(1);\n        console.error(  [10/10] ✗ Functional tests failed (s): );\n        throw error;\n      });\n      console.log(  [10/10] ✓ Functional tests completed (s));";
fs.writeFileSync(file, txt.replace(regex, replacement),'utf8');
